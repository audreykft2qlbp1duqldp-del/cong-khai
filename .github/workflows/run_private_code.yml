name: Run private main.py (public repo only)

on:
  workflow_dispatch:
    inputs:
      REF:
        description: "Branch/Tag của repo riêng tư"
        required: true
        default: "main"
      FILE_PATH:
        description: "Đường dẫn tới main.py trong repo riêng tư"
        required: true
        default: "src/main.py"
      PYTHON_VERSION:
        description: "Python version để chạy"
        required: true
        default: "3.10"

jobs:
  run-main:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    permissions: {}   # dùng PAT riêng để checkout repo private
    steps:
      - name: Checkout (public repo)
        uses: actions/checkout@v4

      - name: Checkout (private repo, sparse to 1 file)
        uses: actions/checkout@v4
        with:
          repository: audreykft2qlbp1duqldp-del/hoanthien   # ← ĐỔI
          ref: ${{ inputs.REF }}
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}
          path: private-src
          fetch-depth: 1
          # Chỉ lấy đúng 1 file (cần tắt cone mode để match file)
          sparse-checkout: |
            ${{ inputs.FILE_PATH }}
          sparse-checkout-cone: false

      - name: Setup Python ${{ inputs.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.PYTHON_VERSION }}

      - name: Run main.py từ repo riêng tư
        shell: bash
        run: |
          set -euo pipefail
          FILE="private-src/${{ inputs.FILE_PATH }}"
          if [[ ! -f "$FILE" ]]; then
            echo "::error::Không tìm thấy file: $FILE"
            ls -la private-src || true
            exit 1
          fi

          echo "[INFO] Running: $FILE"
          python "$FILE"
